namespace QuickLaunchMenuWinForms.Models
{
    /// <summary>Which right-click menu this manages: empty desktop, a folder itself, or any file.</summary>
    public enum MenuScope
    {
        Desktop,
        Folder,
        File
    }
}
